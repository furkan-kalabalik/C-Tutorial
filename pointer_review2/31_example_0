Create a struct called Complex using typedef that contains two fields:
    int real
    int imaginary

Then create a function called printComplex that takes a Complex object pointer as a 
parameter and prints out the real and imaginary parts of the complex number.

void printComplex(Complex *c)
{
    //Fill Here

}

Create a function called incrementReal that takes a Complex object pointer as a parameter
and increments the real part of the complex number.

void incrementReal(Complex *c)
{
    //Fill Here

}

Create a function called addComplex that takes two Complex object pointers as parameters
and adds the two complex numbers and puts the result in the first Complex object.

void addComplex(Complex *c1, Complex *c2)
{
    //Fill Here

}

----MAIN---
    Complex complex1;
    Complex complex2;

    complex1.real = 1;
    complex1.imaginary = 2;
    complex2.real = 3;
    complex2.imaginary = 4;

    printComplex(?); 1+2i
    printComplex(?); 3+4i

    incrementReal(?); 2+2i for complex1
    printComplex(?); 2+2i for complex1

    addComplex(?, ?); 5+6i for complex1
    printComplex(?); 5+6i for complex1

